## MatchPet (Demo Frontend)

Este repositorio contiene una demo frontend construida con Parcel.

### Ejecutar

```bash
npm install
npm start
```

### Datos / autenticación

- La demo no usa Firebase ni backend.
- Usuarios y mascotas se guardan en `localStorage`.
- Para limpiar el estado, borra `localStorage` en el navegador.
